---
description: Bootstrap a fresh workshop project — copy the Lobby Radar starter code from the plugin cache into the current directory, init git, and run smoke check.
---

Run the `init` skill using the instructions at `skills/init/SKILL.md`. Follow every gate exactly (empty-CWD check, Python-on-PATH check, plugin cache resolution). Do not skip refusal cases.
